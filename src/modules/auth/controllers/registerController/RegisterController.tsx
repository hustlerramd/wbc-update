import * as React from "react";

type RegisterControllerProps = {
  //
};

const RegisterController: React.FC<any> = () => {
  return <div>RegisterController</div>;
};

export default RegisterController;
